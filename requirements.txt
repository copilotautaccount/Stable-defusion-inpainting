# Core deep learning
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# Hugging Face ecosystem
diffusers>=0.27.0
transformers>=4.49.0,<5.0.0
accelerate>=0.28.0
datasets>=2.18.0
huggingface_hub>=0.21.0

# LoRA / parameter-efficient fine-tuning
peft>=0.10.0

# Image processing
Pillow>=10.0.0
opencv-python>=4.9.0
scikit-image>=0.22.0
albumentations>=1.4.0

# Auto-masking – choose one or more backends:
#
#   SAM v1 (Meta, requires local .pth checkpoint):
segment-anything>=1.0
#
#   SAM 2 (Meta, loads from HuggingFace automatically):
#   pip install 'git+https://github.com/facebookresearch/sam2.git'
#
#   SAM 3 (Meta, text-prompted concept segmentation, loads from HuggingFace):
#   pip install sam3
#
#   Grounded-SAM (GroundingDINO + SAM, text-prompted furniture detection):
#   pip install groundingdino-py
#   NOTE: groundingdino-py requires a CUDA-capable GPU and torch with CUDA.
#
#   OneFormer (panoptic segmentation, already covered by transformers>=4.38)

# Caption generation – three backends, all via transformers:
#   BLIP  (Salesforce/blip-image-captioning-large, ~6 GB VRAM)  ← no extra dep
#   BLIP-2 (Salesforce/blip2-opt-2.7b, ~15 GB VRAM)             ← no extra dep
#   Florence-2 (microsoft/Florence-2-large, ~8 GB VRAM)          ← no extra dep

# Data utilities
numpy>=1.24.0
pandas>=2.0.0
tqdm>=4.66.0
pycocotools>=2.0.7

# Configuration
omegaconf>=2.3.0
pyyaml>=6.0

# 8-bit optimisers (optional – reduces VRAM usage during training)
# Enable via  use_8bit_adam: true  in configs/train_config.yaml
bitsandbytes>=0.43.0

# Experiment tracking (optional but recommended)
wandb>=0.16.0
tensorboard>=2.15.0

# Memory-efficient attention
xformers>=0.0.24

# Evaluation metrics
lpips>=0.1.4
torchmetrics>=1.3.0
pytorch-fid>=0.3.0

# Miscellaneous
einops>=0.7.0
scipy>=1.12.0
matplotlib>=3.8.0
seaborn>=0.13.0

# Testing
pytest>=7.0.0
pytest-mock>=3.12.0
